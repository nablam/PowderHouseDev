using UnityEngine;
using System.Collections;

public class controlerScriptPosition : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
transform.position = new Vector3 (-.3f,0,-3f);
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
