using UnityEngine;

public class CameraPointSelfManaged : MonoBehaviour
{
    Vector3 OriginalPos = new Vector3(0f, 1.3f, -3f);
    // Start is called before the first frame update
    void Start()
    {
        // transform.position = OriginalPos;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
